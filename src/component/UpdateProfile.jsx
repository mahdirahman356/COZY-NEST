import { Helmet } from "react-helmet-async";

const UpdateProfile = () => {
    return (
        <div>
            <Helmet>
                <title>CozyNest | UpdateProfile</title>
            </Helmet>
            <p>Update profaile</p>
        </div>
    );
};

export default UpdateProfile;